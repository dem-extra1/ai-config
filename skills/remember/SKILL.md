---
name: remember
description: "Alias for `memorize`. Persist a fact or preference to memory, routed by relevance to project-specific or general scope. Use when the user says '/remember', 'remember that …', 'from now on …', 'always/never …', or 'note that …'."
user-invocable: true
allowed-tools:
  - Read
  - Write
  - Edit
  - Bash
---

# remember (alias for `memorize`)

`remember` and `memorize` are synonyms — same behavior regardless of which word
the user uses. Read and follow the canonical skill:

→ **`~/.claude/skills/memorize/SKILL.md`**
